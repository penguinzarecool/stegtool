This is the bash version. Due to bash, the input is very short, so if you put a longer message in, it will crash.
Will probably later edit it so that you can put your message in a file instead of in a variable.
